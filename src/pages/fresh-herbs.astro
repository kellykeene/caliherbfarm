---
/**
 * The Harvest Calendar page used to live here. Permanent, so search engines
 * move the page's ranking to the new address and old links keep working.
 */
return Astro.redirect('/harvest-calendar', 301);
---
